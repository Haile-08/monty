#include "monty.h"

/**
 * nop - nop
 * @stack: the top of the pointer
 * @line_number: the number of the line
 *
 * Description: don't do any thing
 * Return: none
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
